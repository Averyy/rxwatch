# Database (PostgreSQL via Docker)
# For local scripts (outside Docker) - uses exposed port 5433
DATABASE_URL=postgres://rxwatch:your_secure_password@localhost:5433/rxwatch

# Docker Postgres config (used by docker-compose.prod.yml)
POSTGRES_USER=rxwatch
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=rxwatch

# Health Product Shortages Canada API (DSC)
# Register for free accounts at https://www.healthproductshortages.ca/
# Multiple accounts for rate limit failover (1000 req/hour per IP)
DSC_API_URL=https://www.healthproductshortages.ca/api/v1
DSC_ACCOUNTS='[{"email":"your-email@example.com","password":"your-password"}]'

# Health Canada DPD API (no auth required)
DPD_API_URL=https://health-products.canada.ca/api/drug

# App URL (for production)
NEXT_PUBLIC_APP_URL=http://localhost:5000

# Cron security (generate with: openssl rand -hex 32)
CRON_SECRET=generate-a-random-string-here

# Error notifications (optional - Discord/Slack webhook URL)
# NOTIFY_WEBHOOK_URL=https://discord.com/api/webhooks/...
